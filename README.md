This bash script file is and exe file that does as follows 

● Display the current date and time.
● Display the system uptime.
● Display the currently logged-in users.
● Display the disk usage of the root filesystem.
